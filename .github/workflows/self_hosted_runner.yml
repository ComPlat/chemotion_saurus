name: self-hosted runner

on:
  workflow_dispatch:
  push:
    branches: [ master ]

jobs:
  docker:
    if: ${{ true }}
    runs-on: self-hosted
    steps:
    - uses: docker://complat/complat-ubuntu-runner:0.10.20
      with:
        args: docker exec -i $(docker run -it -d complat/complat-ubuntu-runner:0.10.20) bash
    - run: |
          ls
      #working-directory: ${{GITHUB_WORKSPACE}}
      # working-directory: /home/ira/actions-runner/chemotion_saurus_github/chemotion_saurus/chemotion_saurus/
