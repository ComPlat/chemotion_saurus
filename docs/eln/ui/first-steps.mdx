---
title: First Steps
sidebar_position: 1
---

import { faCog, faPlus, faSearch } from "@fortawesome/free-solid-svg-icons";
import { Btn } from "@site/src/js/btn.js";

## Registration

Registration is a necessary step towards using the electronic laboratory notebook (ELN). Please register on the first visit to the ELN. Then use this access data for each subsequent login. The registration page or the login page will always be the first page accessed.

## SciFinder/SciFinder<sup>n</sup> Credential

This step is only required if the user has been granted permission to use the [Search CAS](../ui/details#search-cas) (<Btn mixed={[faSearch, "Search CAS"]} color={"secondary"} />) feature (ELN 1.3+ versions) or the SciFinder tab feature (prior to ELN 1.0.0).
Both of these features query information in SciFinder/SciFinder<sup>n</sup> by using an API.

:::info[Note]

Please note that you must register once with CAS to obtain your personal CAS username and password.

:::

Users can register the SciFindern credential in the "Account & Profile".

!["SciFinderN Profile"](/img/eln/ui/first_steps/scifindern_profile.png)

### ELN 1.3 and above (Search CAS)

:::info[Note]

This function is not accessible to all users, please ask your instance administrator if you don't see this function.

:::

![CAS Search](/img/eln/ui/first_steps/scifindern_function.png)

When the user first sees the SciFinder-n Credential, the expiry date is blank.

![SciFinderN No Credential](/img/eln/ui/first_steps/sciFinder-n-credential.png)

After pressing the <Btn mixed={["Get token"]} color={"primary"} /> button, the user is redirected to the CAS login page. Please enter the CAS username and password to request an access token.

![SciFinderN CAS Login](/img/eln/ui/first_steps/scifindern_cas_login.png)

If a valid expiry date is shown, the user is ready to use the Search CAS feature. Please see [Search CAS](../ui/details#search-cas).

![SciFinderN Credential](/img/eln/ui/first_steps/scifindern_has_credential.png)

### Prior to ELN 1.0.0 (SciFinder tab)

![SciFinder Tab](/img/eln/ui/first_steps/scifinder_tab.png)

When using a version of ELN prior to 1.0.0, users can register the SciFinder credential in the "Account & Profile".

<video controls>
  <source src={require("@site/static/img/eln/ui/first_steps/scifinder_register.webm").default} />
</video>

After pressing the <Btn mixed={["Submit"]} color={"info"}/> button, a token will show on the page if the SciFinder credential is valid.

![SciFinder Token](/img/eln/ui/first_steps/scifinder_token.png)

If the credential is not valid, "none" will be shown in the column "current token".

![SciFinder No_Token](/img/eln/ui/first_steps/scifinder_no_token.png)

## The First Collection

<video controls>
  <source src={require("@site/static/img/eln/ui/first_steps/first_collection.webm").default} />
</video>

A **collection** is a collection of elements ([**Sample**](/docs/eln/ui/elements/samples), [**Reaction**](/docs/eln/ui/elements/reactions), [**Wellplate**](/docs/eln/ui/elements/wellplates), **Screen**, **Research Plan** and others) that is assigned to a project or subproject. Two collections are given per default: **chemotion-repository.net** and **All**. The **chemotion-repository.net** collection is a connector to the Chemotion repository and should not be used in the first Chemotion ELN trials. The collection **All** is installed as backup, it contains all entries of the Chemotion ELN independent of other assignments. The creation of the first own collection is usually the first step in the Chemotion ELN and allows users to store their own elements.
For the creation of collections please use the [collection management](/docs/eln/ui/collections) via the red button <Btn mixed={[faCog]} color={"danger"}/> in the collection bar. Please create a new entry **New Collection** under **My Collections** by pressing <Btn mixed={[faPlus]} color={"success"}/>. Please rename the collection and save it by pressing <Btn mixed={["Save"]} color={"warning"}/>.

:::danger[Caution]

To use the Chemotion ELN functions, a first user-defined collection has to be created!
Please think about a smart structure for your Chemotion ELN and create your first collection!

:::

## Chemotion ELN user settings

### User Settings: Account & Profile

The account & profile settings allow the user to

- define a reaction label and adjust the counter for reactions
- define a sample inventory label and adjust the inventory label counter for a collection
- change the visibility of labels/numbers for samples
- choose a curation standard for the analytical section
- [add SciFinder/SciFinder<sup>n</sup> credentials](#scifinderscifindern-credential)
- adjust the counter for different elements in the Chemotion ELN
- set up their default structure editor

## Define a sample inventory label and adjust the counter inventory label for a collection

Users can select one or multiple collections and assign a name, prefix, and starting counter to define an inventory label for the collection(s).
Any changes made can be saved by clicking the "Update User Settings" button within the "Sample Inventory Label" box.
Afterward, users can return to the Chemotion main page, where the inventory label will appear next to the collection name in the collections tree.

When creating a new sample, a sample inventory label will automatically be generated with the next available counter number.
Additionally, users can generate a new label for existing samples by clicking the "Auto-Generate Inventory Label" button next to the inventory label field.

<video controls>
  <source src={require("@site/static/img/eln/ui/first_steps/inventory_label_assignment.webm").default} />
</video>


### User Settings: Template management

This function is not accessible to all users, please ask the instance admin if it is not visible

### User Settings: Change Password

### User Settings: My affiliations

### User Settings: My groups

### User Settings: Molecule Moderator

This function is not accessible to all users, please ask the instance admin if it is not visible

### User Settings: My devices

This function is not accessible to all users, please ask the instance admin if it is not visible
