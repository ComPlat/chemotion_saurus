---
title: Text Editor
---

import { faTrashAlt } from "@fortawesome/free-solid-svg-icons";
import { Btn } from "@site/src/js/btn.js";

Text editors appear throughout the ELN, and text templates let users insert ready-made paragraphs with a single click — handy for content written repeatedly.
Everyone can create their own personal templates. Global templates (shared across the whole ELN) can be viewed by anyone and edited by users with the [Global Template Editor](../admin/user-management#global-text-template-editor-rights) privilege.

![](/img/eln/ui/text_editor/20231013_customTextblocks.png)

**Formatting utilities**: standard text formatting (bold, italic, lists, etc.).<br />
**User-defined custom text templates:** icons or dropdown menus for personal templates or global templates.<br />
**Configuration**: pop-up menu to configure `text templates` in the editor.

## Using and customizing icons

Users can use predefined templates and their own templates when the cursor is active in the editor.
Click an icon to insert the `text template` content at the cursor position.

The following video shows how to customize the toolbar:

<video controls>
  <source src={require("@site/static/img/eln/ui/text_editor/20231013_configTextTemplatesIcon.webm").default} />
</video>

## Using and customizing templates dropdown menus

Users can also group templates into dropdown menus instead of individual icons:

<video controls>
  <source src={require("@site/static/img/eln/ui/text_editor/20231013_configTextTemplatesDropDown.webm").default} />
</video>

Click <Btn mixed={[faTrashAlt]} color={"danger"}/> to remove any item from the toolbar or dropdown.

:::info
This feature is available starting from version 4.0.
:::
## Text Templates

The **MT dropdown** in the toolbar can show predefined templates as well as personal templates and new global templates. Open the toolbar settings menu and click [Text Templates](./text-templates) to manage the templates that appear there.

![MT dropdown](/img/eln/ui/text-templates/mt-dropdown.png)

